# CONCRETE_DOMAIN建表

## PostgreSQL版本

```sql
DROP TABLE "CONCRETE_DOMAIN";
CREATE TABLE "CONCRETE_DOMAIN" (
    "ID" NUMERIC(22) PRIMARY KEY,
    "INTEGERS" INTEGER[],
    "LONGS" BIGINT[],
    "BIG_DECIMALS" NUMERIC(22)[],
    "STRINGS" TEXT[],
    "CONCRETE_ELEMENT_A" JSONB,
    "CONCRETE_ELEMENT_A_LIST" JSONB,
    "CONCRETE_ELEMENT_B" JSONB,
    "CONCRETE_ELEMENT_B_LIST" JSONB,
    "CREATED_AT" TIMESTAMP,
    "UPDATED_AT" TIMESTAMP
);

-- 创建GIN索引用于全文搜索和JSON查询
CREATE INDEX "IDX_CONCRETE_DOMAIN_01" ON "CONCRETE_DOMAIN" USING GIN("SEARCH_VECTOR");
```

