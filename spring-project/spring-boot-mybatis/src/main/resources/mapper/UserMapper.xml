<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.maiaimei.example.mapper.UserMapper">
    <select id="get" resultType="cn.maiaimei.example.entity.User">
        select id, username, password, nickname
        from sys_user
        where id = #{id}
    </select>

    <select id="listAll" resultType="cn.maiaimei.example.entity.User">
        select id, username, password, nickname
        from sys_user
    </select>

    <select id="pageQuery" resultType="cn.maiaimei.example.entity.User">
        select id, username, password, nickname
        from sys_user limit #{from},#{size}
    </select>
</mapper>