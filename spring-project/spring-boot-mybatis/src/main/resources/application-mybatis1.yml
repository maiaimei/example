mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    #log-impl: org.apache.ibatis.logging.stdout.StdOutImpl 在控制台打印sql
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl # 在文件中打印sql
